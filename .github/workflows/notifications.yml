name: Notificaciones diarias
on:
  workflow_dispatch:
    inputs:
      type:
        description: "Tipo"
        required: false
        default: "hoy"
        type: choice
        options:
          - hoy
          - manana
          - reminders
  schedule:
    - cron: "*/15 * * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Llamar cron
        env:
          URL: "https://${{ secrets.DEPLOY_URL }}"
          SECRET: "${{ secrets.CRON_SECRET }}"
          INPUT: "${{ github.event.inputs.type }}"
        run: |
          if [ "$INPUT" = "reminders" ]; then
            curl -sSf -H "x-cron-secret: $SECRET" "$URL/api/cron/reminders"
          elif [ "$INPUT" = "manana" ]; then
            curl -sSf -H "x-cron-secret: $SECRET" "$URL/api/cron/agenda?type=manana"
          elif [ "$INPUT" = "hoy" ]; then
            curl -sSf -H "x-cron-secret: $SECRET" "$URL/api/cron/agenda?type=hoy"
          else
            curl -sSf -H "x-cron-secret: $SECRET" "$URL/api/cron/reminders"
            curl -sSf -H "x-cron-secret: $SECRET" "$URL/api/cron/agenda?type=hoy"
            curl -sSf -H "x-cron-secret: $SECRET" "$URL/api/cron/agenda?type=manana"
          fi
